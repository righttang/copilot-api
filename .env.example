# Required API Keys
ANTHROPIC_API_KEY="your-anthropic-api-key" # Needed if proxying *to* Anthropic
OPENAI_API_KEY="dummy-key"  # Can be any value for local OpenAI-compatible APIs

# OpenAI-Compatible API Configuration
OPENAI_API_BASE="http://localhost:4142"  # Your local OpenAI-compatible endpoint

# Optional: Provider Preference and Model Mapping
# Controls which provider (google or openai) is preferred for mapping haiku/sonnet.
# Defaults to openai if not set.
PREFERRED_PROVIDER="openai"

# Use models available on your local API (adjust these to match your available models)
BIG_MODEL="claude-sonnet-4"
SMALL_MODEL="claude-sonnet-4"